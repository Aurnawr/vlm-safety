Just playing around with VLM safety, jailbreaking, alignment etc

Experiment 1: we compare the hidden states of image agnostic vs image aware prompts plus the blank image and visualize this using pca. We also have the generated responses for agnostc vs aware which we can use to check the evaluation


![Plot of image agnostic vs image aware hidden states of the last layer ](/opt/watchdog/users/arnav/vlm-safety/data/pca_with_refusal_direction_lastlayer.png)